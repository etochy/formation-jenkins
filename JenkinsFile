pipeline {
    agent any
    tools {
        maven 'M3'
    }
    stages {
        stage("Checkout") {
            steps {
                git url: "https://github.com/etochy/formation-jenkins.git", branch: "main"
            }
        }
        stage("Build") {
            steps {
                echo "Start build"
                sh "mvn clean compile"
            }
        }
    }
}